from statistics import *

print(mean([1, 2, 3, 4, 5])) # calculates the mean (average) of the data
print(median([1, 2, 3, 4, 5])) # calculates the median (middle value) of the data
print(mode([1, 2, 2, 3, 4, 5])) # calculates the mode (most common value) of the data

